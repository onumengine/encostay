import 'package:flutter/material.dart';

class CardSelectionDialog extends StatelessWidget {
  const CardSelectionDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
